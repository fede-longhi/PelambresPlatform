import {
  checkoutResultMetadata,
  createCheckoutResultPage,
} from '../_components/checkout-result';

export const metadata = checkoutResultMetadata.pending;
export default createCheckoutResultPage('pending');
